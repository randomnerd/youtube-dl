provider:
  name: faas
  gateway: http://127.0.0.1:8080
functions:
  youtubedl:
    lang: Dockerfile
    handler: ./
    image: randomnerd/faas-youtubedl:latest
    environment:
      read_timeout: 300s
      write_timeout: 300s
    labels: 
      com.openfaas.ui.ext: "mp4"
